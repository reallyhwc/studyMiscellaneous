实现一个支持动态扩容的数组

思路： 整体实现逻辑与 java中自带的arrayList类似

实现效果：
1.  可以便捷的根据下标来获取数组中的数据
2.  可以根据下标修改、删除数组中的数据
3.  可以初始化是指定数组中的容量，并随着数据的增加，动态去扩容的添加数据

实现过程中，可以考虑与ArrayList采取类似的操作
底层依旧是采用数组存储的，new 完数组并添加数据的时候，会默认的初始化一个指定容量的大小的数组
随着数据的添加，如果size + 1 > capacity  则会new一个新的数组出来，把老的数据拷贝进新数组，再添加数据
